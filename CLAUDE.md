# Claude Compatibility

This project uses `AGENTS.md` as the canonical agent development guide.
Claude Code automatically loads `CLAUDE.md`, so this file imports the shared guide:

@AGENTS.md

Claude-specific skill compatibility files are mirrored under `.claude/skills`.
The canonical skill files live under `.agents/skills`.
